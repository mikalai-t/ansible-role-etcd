---

- name: "02.1 - download etcd archive"
  get_url:
    url: "{{ __etcd_download_url }}"
    dest: "{{ lookup('env', 'HOME') }}/etcd-${etcd_version}.tar.gz"

- name: "02.2.1 - make sure directory for etcd unpacked files is created"
  file:
    path: "{{ lookup('env', 'HOME') }}/etcd-{{ etcd_version }}"
    state: directory

- name: "02.2.2 - unpack etcd archive"
  shell:
    cmd: "tar -xzf {{ lookup('env', 'HOME') }}/etcd-{{ etcd_version }}.tar.gz -C {{ lookup('env', 'HOME') }}/etcd-{{ etcd_version }} --strip-components=1"
  args:
    warn: no
  environment:
    DEBIAN_FRONTEND: noninteractive
  become: yes

- name: "02.3 - install etcd binaries"
  copy:
    src: "{{ item.src }}"
    dest: "{{ item.dest }}"
    remote_src: yes
    owner: "{{ __etcd_user }}"
    group: "{{ __etcd_group }}"
    mode: "0755"
  become: yes
  loop:
    - src: "{{ lookup('env', 'HOME') }}/etcd-{{ etcd_version }}/etcd"
      dest: "{{ __etcd_binary }}"
    - src: "{{ lookup('env', 'HOME') }}/etcd-{{ etcd_version }}/etcdctl"
      dest: "{{ __etcd_client_binary }}"
